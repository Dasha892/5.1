internal class Program
{
    private static void Main(string[] args)
    {
        int a = 5; // ввод целочисленной переменной a
        int b = 10; // ввод целочисленной переменной b
        int c = 21; // ввод целочисленной переменной c

        Console.WriteLine(a); // вывод на экран строки "..."
        Console.WriteLine(b); // вывод на экран строки "..."
        Console.WriteLine(c); // вывод на экран строки "..."
    }
}